# Optional API Keys (at least one model provider required)
OPENAI_API_KEY=sk-...  # Optional, required for OpenAI models
PERPLEXITY_API_KEY=pplx-...  # Optional, enables research capabilities
TELEGRAM_API_KEY=...  # Optional, enables Telegram posting capabilities

# Optional Settings (with defaults)
LOG_LEVEL=INFO  # Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
DEBUG=False  # Enable verbose logging
DEFAULT_MODEL=gpt-4o-mini  # Default AI model to use
